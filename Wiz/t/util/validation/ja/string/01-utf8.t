#!/usr/bin/perl

use strict;
use warnings;

use lib qw(../../../../../lib);

use Wiz::Constant qw(:common);
use Wiz::Test skip_all => 'todo';
use Wiz::Util::Validation qw(:all);

chtestdir;

sub main {
    return 0;
}

exit main;
